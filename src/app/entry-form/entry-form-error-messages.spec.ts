import { EntryFormErrorMessages } from './entry-form-error-messages';

describe('EntryFormErrorMessages', () => {
  it('should create an instance', () => {
    expect(new EntryFormErrorMessages()).toBeTruthy();
  });
});
